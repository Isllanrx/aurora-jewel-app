# dependencies
node_modules/
.pnp
.pnp.js

# Expo
.expo/
dist/
web-build/

# Android
android/
ios/

# env files (NEVER commit credentials)
.env
.env.local
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# misc
.DS_Store
*.pem

# Next.js leftovers (not used in Expo project)
public/

# Binary image assets — servidos via GitHub raw URL (lib/assets.js)
assets/

# AI agent config files — usados localmente, não necessários no Snack
.claudeignore
.geminiignore

# SQL schema — ferramenta de desenvolvimento, não necessário no Snack
supabase_schema.sql
