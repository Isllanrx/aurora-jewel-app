# dependencies
node_modules/
package-lock.json
.pnp
.pnp.js

# Expo
.expo/
dist/
web-build/

# Android
android/
ios/

# env files (NEVER commit credentials)
.env
.env.local
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# misc
.DS_Store
*.pem

# Next.js leftovers (not used in Expo project)
public/

# Binary image assets — servidos via GitHub raw URL (lib/assets.js)
assets/

# AI agent config files — usados localmente, não necessários no Snack
.claudeignore
.geminiignore

# SQL schema legado e migrations — não necessários no Snack
supabase_schema.sql
supabase/

# Arquivos de exemplo de configuração
.env.example

# Markdown — docs de dev/AI, não necessários no Snack
# README.md e supabase/README.md são mantidos rastreados
*.md
!README.md
!supabase/README.md
